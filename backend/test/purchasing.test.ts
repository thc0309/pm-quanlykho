import assert from"node:assert/strict";import test from"node:test";test("purchase outstanding quantity never permits over receipt",()=>{const outstanding=(ordered:number,received:number)=>ordered-received;assert.equal(outstanding(10,4),6);assert.equal(7>outstanding(10,4),true);});test("purchase receipt idempotency is keyed by warehouse document number",()=>{const key=(w:string,n:string)=>`${w}:${n}`;assert.equal(key("a","R1"),key("a","R1"));assert.notEqual(key("a","R1"),key("b","R1"));});
