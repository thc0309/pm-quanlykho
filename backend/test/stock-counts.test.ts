import assert from"node:assert/strict";import test from"node:test";import{countVariance}from"../src/modules/stock-counts.js";test("stock count posts positive and negative variance only",()=>{assert.equal(countVariance(5,7),2);assert.equal(countVariance(5,3),-2);assert.equal(countVariance(5,5),0)});test("snapshot timestamp detects concurrent movement",()=>assert.notEqual(new Date("2026-01-01").getTime(),new Date("2026-01-02").getTime()));
