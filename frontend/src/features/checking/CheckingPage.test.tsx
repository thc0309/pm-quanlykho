import"@testing-library/jest-dom/vitest";import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{expect,it,vi}from"vitest";import type{CheckingClient}from"../../lib/api";import CheckingPage from"./CheckingPage";it("independently scans and ships",async()=>{const api:CheckingClient={list:vi.fn().mockResolvedValue([{id:"o",documentNo:"OUT-1",status:"picked",checkerUserId:null,version:3}]),claim:vi.fn().mockResolvedValue({resumed:false,version:4}),scan:vi.fn().mockResolvedValue({checked:1,required:1}),ship:vi.fn().mockResolvedValue({alreadyShipped:false})};const u=userEvent.setup();render(<CheckingPage api={api}/>);await u.click(await screen.findByRole("button",{name:"Nhận kiểm"}));await u.type(screen.getByLabelText("Barcode staging"),"STAGE");await u.type(screen.getByLabelText("Barcode sản phẩm / lô / serial"),"LOT-A");await u.click(screen.getByRole("button",{name:"Lưu lần kiểm"}));expect(await screen.findByLabelText("Tiến độ kiểm")).toHaveTextContent("1 / 1");await u.click(screen.getByRole("button",{name:"Xác nhận và xuất kho"}));expect(api.ship).toHaveBeenCalled();});
