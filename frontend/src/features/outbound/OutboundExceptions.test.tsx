import"@testing-library/jest-dom/vitest";import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{expect,it,vi}from"vitest";import type{OutboundExceptionClient}from"../../lib/api";import OutboundExceptions from"./OutboundExceptions";it("cancels an unpicked document through an action API",async()=>{const api:OutboundExceptionClient={list:vi.fn().mockResolvedValue([{id:"o",documentNo:"OUT-1",status:"ready_to_pick",pickerUserId:null,checkerUserId:null,version:1}]),mismatch:vi.fn(),approveShort:vi.fn(),cancel:vi.fn(),reassign:vi.fn()};const u=userEvent.setup();render(<OutboundExceptions api={api}/>);await u.click(await screen.findByRole("button",{name:"Hủy phiếu"}));expect(api.cancel).toHaveBeenCalledWith("o","Xử lý ngoại lệ theo xác nhận");expect(screen.queryByText("OUT-1")).not.toBeInTheDocument();});
