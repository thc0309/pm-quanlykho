import"@testing-library/jest-dom/vitest";import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{MemoryRouter,Route,Routes}from"react-router";import{expect,it,vi}from"vitest";import type{PrintClient}from"../../lib/api";import PrintPage from"./PrintPage";it("reprint only opens system print and calls no stock mutation",async()=>{const api:PrintClient={document:vi.fn().mockResolvedValue({document:{id:"d",documentNo:"RCV-1",documentType:"receipt",status:"confirmed",confirmedAt:"",partnerName:null,warehouseName:"MAIN"},lines:[]}),label:vi.fn()},spy=vi.spyOn(window,"print").mockImplementation(()=>{}),u=userEvent.setup();render(<MemoryRouter initialEntries={["/print/d"]}><Routes><Route path="/print/:id" element={<PrintPage api={api}/>}/></Routes></MemoryRouter>);await u.click(await screen.findByRole("button",{name:"In hệ thống"}));expect(spy).toHaveBeenCalledTimes(1);expect(api.document).toHaveBeenCalledTimes(1);spy.mockRestore()});
