import"@testing-library/jest-dom/vitest";import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{expect,it,vi}from"vitest";import type{ReportClient}from"../../lib/api";import ReportsPage from"./ReportsPage";it("filters and exposes bounded CSV export",async()=>{const api:ReportClient={dashboard:vi.fn(),inventory:vi.fn().mockResolvedValue({data:[{sku:"SKU-A",productName:"A",locationCode:"L",lotCode:null,serialCode:null,onHand:3}],pagination:{page:1,pageSize:20,totalItems:1,totalPages:1}}),exportUrl:vi.fn(q=>`/export?q=${q}`)};const u=userEvent.setup();render(<ReportsPage api={api}/>);expect(await screen.findByText("SKU-A")).toBeTruthy();await u.type(screen.getByLabelText("Tìm báo cáo"),"SKU");await u.click(screen.getByRole("button",{name:"Lọc"}));expect(api.inventory).toHaveBeenLastCalledWith({page:1,q:"SKU"});expect(screen.getByRole("link",{name:"Export CSV"})).toHaveAttribute("href","/export?q=SKU");});
